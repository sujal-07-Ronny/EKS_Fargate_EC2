apiVersion: apps/v1
kind: Deployment
metadata:
  name: ec2-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ec2-nginx
  template:
    metadata:
      labels:
        app: ec2-nginx
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80